[package]
name = "RustApi"
version = "0.1.1"
edition = "2021"

[dependencies]
rocket = { version = "0.5.1-rc.2", features = ["json"]}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"